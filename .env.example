# Copiar para .env (chmod 600 na VPS) e preencher.
JWT_SECRET=

# Ferramenta "Finanças": opcional — sem chave, usa o extrator heurístico local
# em vez do LLM (Anthropic Claude) para interpretar os lançamentos.
ANTHROPIC_API_KEY=
