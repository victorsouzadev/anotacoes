{
  "name": "@anotacoes/mcp-tasks",
  "version": "2.2.0",
  "description": "Servidor MCP para adicionar e gerenciar tarefas do hub via LLM (Claude Desktop, Claude Code, etc.)",
  "private": true,
  "type": "module",
  "bin": {
    "anotacoes-tasks-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.10.5",
    "vitest": "^3.0.5"
  }
}
